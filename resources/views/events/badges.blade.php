<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Badge studio · {{ $event->title }}</title>
<style>
@include('events.partials.badge-style')
*{box-sizing:border-box}body{margin:0;background:#eef2f4;color:#17323b;font-family:Arial,sans-serif;font-size:14px}button,input,select{font:inherit}button,a,input,select{outline-offset:3px}button{cursor:pointer}.topbar{background:#12333d;color:#fff;padding:18px 28px;display:flex;align-items:center;justify-content:space-between;gap:20px}.topbar h1{font-size:21px;margin:0}.topbar p{color:#b9d3d7;margin:6px 0 0}.topbar a{color:#fff}.workspace{display:grid;grid-template-columns:320px minmax(420px,1fr) 290px;gap:20px;padding:24px;max-width:1600px;margin:auto}.panel{background:#fff;border:1px solid #dbe4e7;border-radius:14px;padding:20px;margin-bottom:16px}.panel h2{font-size:16px;margin:0 0 14px}.panel h3{font-size:13px;margin:18px 0 8px}label{display:block;font-size:12px;font-weight:700;color:#425e67;margin:12px 0 6px}input:not([type=checkbox]):not([type=color]),select{width:100%;border:1px solid #cbd9dd;border-radius:7px;padding:9px;background:white;color:#17323b}input[type=color]{width:100%;height:36px;border:1px solid #cbd9dd;background:white;border-radius:6px}.row{display:grid;grid-template-columns:1fr 1fr;gap:12px}.help{font-size:12px;line-height:1.6;color:#627981;margin:8px 0}.btn{display:inline-block;border:0;background:#0f766e;color:white;border-radius:8px;padding:11px 15px;font-weight:700;text-decoration:none;text-align:center}.btn.secondary{background:#eaf1f3;color:#21444e}.btn:disabled{opacity:.45;cursor:not-allowed}.wide{width:100%;margin-top:12px}.stage-head{display:flex;justify-content:space-between;align-items:center;gap:15px;margin-bottom:14px}.stage-head label{margin:0}.stage-head select{max-width:260px}.canvas-wrap{background:#dce5e8;background-image:radial-gradient(#b7c9ce 1px,transparent 1px);background-size:16px 16px;border:1px solid #c8d6da;border-radius:14px;padding:30px 12px;overflow:auto;display:flex;justify-content:center;min-height:650px}.badge{flex-shrink:0;box-shadow:0 8px 30px #183a4026}.badge-field{cursor:move;touch-action:none}.badge-field:hover,.badge-field:focus{outline:1px dashed #0d9488}.badge-field.selected{outline:2px solid #0d9488;outline-offset:2px}.guides .badge:after{content:'';position:absolute;inset:5mm;border:1px dashed #38bdf8;pointer-events:none}.guides .badge:before{content:'';position:absolute;left:50%;top:0;bottom:0;border-left:1px dashed #38bdf870;pointer-events:none;z-index:5}.status{font-size:12px;color:#627981}.notice{margin:16px 24px;padding:13px 18px;border-radius:9px;background:#dcfce7;color:#166534}.errors{background:#fff1f2;color:#9f1239}.warning{background:#fff8e6;border:1px solid #ead5a0;padding:12px;border-radius:8px;font-size:12px;line-height:1.6;margin-top:14px}.warning:empty{display:none}.check{display:flex;gap:8px;align-items:center;font-weight:normal;font-size:13px}.attendee-list{max-height:190px;overflow:auto;border-top:1px solid #e2e8f0;margin-top:12px}.attendee-list label{padding:4px 0;margin:4px 0}.eyebrow{letter-spacing:.14em;text-transform:uppercase;color:#66858e;font-size:10px;font-weight:bold;margin:0 0 8px}.field-note{border-top:1px solid #e1e9eb;padding-top:12px;margin-top:16px}.screen-only{font-size:11px;text-align:center;color:#617d86;margin-top:12px}.templates{margin:0 24px 24px;max-width:800px}.hidden{display:none!important}@media(max-width:1150px){.workspace{grid-template-columns:280px minmax(420px,1fr)}.right{grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr;gap:20px}}@media(max-width:760px){.topbar{align-items:flex-start;flex-direction:column}.workspace{display:flex;flex-direction:column;padding:12px}.right{display:block}.canvas-wrap{justify-content:flex-start;min-height:0}.templates{margin:12px}.stage-head{flex-wrap:wrap}}
</style></head><body>
<header class="topbar"><div><h1>Badge studio</h1><p>{{ $event->title }} · {{ $registrations->count() }} confirmed attendees</p></div><a href="{{ route('events.registrations.index', $event) }}">← Back to attendees</a></header>
@if(session('success'))<div class="notice" role="status">{{ session('success') }}</div>@endif
@if($errors->any())<div class="notice errors" role="alert">@foreach($errors->all() as $error)<div>{{ $error }}</div>@endforeach</div>@endif
<form id="design-form" method="POST" action="{{ route('events.badges.settings', $event) }}" enctype="multipart/form-data">
@csrf @method('PATCH')
<div class="workspace">
<aside>
<section class="panel"><p class="eyebrow">01 · Design</p><h2>Customize Badge</h2>
<label for="layout">Starting design</label><select name="badge_layout" id="layout">@foreach(['standard'=>'Signature — colour band','minimal'=>'Minimal — clean white','background'=>'Upload company design','image_header'=>'Artwork header','split'=>'Legacy artwork design'] as $value=>$label)<option value="{{ $value }}" @selected(old('badge_layout', $event->badge_layout ?? 'standard') === $value)>{{ $label }}</option>@endforeach</select>
<p class="help">Use a ready-made design or place attendee fields over your company's artwork.</p>
<div class="row"><div><label for="badge-size">Badge size</label><select name="badge_size" id="badge-size"><option value="A6" @selected(old('badge_size',$event->badge_size)==='A6')>A6 · 105 × 148 mm</option><option value="A5" @selected(old('badge_size',$event->badge_size)==='A5')>A5 · 148 × 210 mm</option></select></div><div><label for="font">Typeface</label><select name="badge_font" id="font">@foreach(['DejaVu Sans'=>'Sans serif','DejaVu Serif'=>'Serif','DejaVu Sans Mono'=>'Monospace'] as $value=>$label)<option value="{{ $value }}" @selected(old('badge_font',$event->badge_font ?? 'DejaVu Sans')===$value)>{{ $label }}</option>@endforeach</select></div></div>
<div class="row"><div><label for="primary">Primary colour</label><input type="color" id="primary" name="badge_primary_color" value="{{ old('badge_primary_color',$event->badge_primary_color ?? '#0F766E') }}"></div><div><label for="accent">Text colour</label><input type="color" id="accent" name="badge_accent_color" value="{{ old('badge_accent_color',$event->badge_accent_color ?? '#0F172A') }}"></div></div>
<label for="name-format">Attendee names</label><select id="name-format" name="badge_name_format"><option value="full" @selected(old('badge_name_format',$event->badge_name_format ?? 'full')==='full')>Show full name</option><option value="initials" @selected(old('badge_name_format',$event->badge_name_format)==='initials')>Use middle initials</option></select>
<label for="category-treatment">Category colour</label><select id="category-treatment" name="badge_design"><option value="default" @selected(old('badge_design',$event->badge_design)!=='category')>Use primary colour</option><option value="category" @selected(old('badge_design',$event->badge_design)==='category')>Colour by category</option></select>
<div id="category-colors">@foreach($categories as $category)<label>{{ $category }}<input type="hidden" name="categories[]" value="{{ $category }}"><input type="color" name="colors[]" data-category="{{ $category }}" value="{{ $categoryColors[$category] }}"></label>@endforeach</div>
<button type="button" id="reset-fields" class="btn secondary wide">Reset field positions</button>
</section>
<section class="panel" id="artwork-panel"><h2>Company artwork</h2><label for="artwork">Upload PNG, JPG or WebP</label><input type="file" id="artwork" name="badge_image" accept="image/png,image/jpeg,image/webp"><p class="help">Maximum 4 MB. For a full background, export at the badge's proportions and leave space for names and the QR code. Artwork fills the badge without cropping.</p><p class="help" id="resolution-help"></p>
@if($event->badge_image_path)<label class="check"><input type="checkbox" id="remove-artwork" name="remove_badge_image" value="1"> Remove saved artwork</label>@endif
<div id="crop-controls"><label>Horizontal crop position<input name="badge_image_position_x" type="range" min="0" max="100" value="{{ $event->badge_image_position_x ?? 50 }}"></label><label>Vertical crop position<input name="badge_image_position_y" type="range" min="0" max="100" value="{{ $event->badge_image_position_y ?? 50 }}"></label></div>
<div id="art-warning" class="warning" role="status"></div></section>
</aside>
<main>
<div class="stage-head"><div><p class="eyebrow">02 · Arrange</p><strong>Live preview</strong></div><select id="preview-attendee" aria-label="Preview attendee">@forelse($registrations as $registration)<option value="{{ $registration->id }}">{{ $registration->participant->name }}</option>@empty<option value="sample">Sample attendee</option>@endforelse</select></div>
<div class="canvas-wrap" id="canvas-wrap">@include('events.partials.badge', ['registration'=>$registrations->first()])</div>
<div class="stage-head"><label class="check"><input type="checkbox" id="guides" checked> Show safe-area and centre guides</label><span id="dimensions" class="status"></span></div>
<p class="screen-only">Drag a field to move it. Select a field to change its size or alignment. Guides do not print.</p>
<div id="layout-warnings" class="warning" role="status"></div>
<button class="btn secondary wide" type="button" id="review-all">Check all attendee names and fields</button><div id="review-result" class="help" role="status"></div>
</main>
<aside class="right">
<section class="panel"><p class="eyebrow">Fields on badge</p><h2>Choose what appears</h2><p class="help">Tick what you want printed &mdash; for example, turn off Room assignment for events without accommodation.</p>
<div id="field-visibility-list">@foreach(\App\Support\BadgeDesign::LABELS as $key=>$label)<label class="check"><input type="checkbox" class="field-visibility-toggle" data-field="{{ $key }}" @if($key==='qr') disabled checked @endif> {{ $label }}@if($key==='qr') <span class="help" style="margin:0">(required)</span>@endif</label>@endforeach</div>
</section>
<section class="panel"><p class="eyebrow">Field inspector</p><h2>Position &amp; typography</h2><label for="field-picker">Selected field</label><select id="field-picker">@foreach(\App\Support\BadgeDesign::LABELS as $key=>$label)<option value="{{ $key }}" @selected($key==='name')>{{ $label }}</option>@endforeach</select>
<label class="check"><input id="field-visible" type="checkbox"> Show on badge</label>
<label class="check"><input id="field-bold" type="checkbox"> Bold</label>
<label class="check"><input id="field-custom-color" type="checkbox"> Custom text colour</label>
<input type="color" id="field-color">
<div class="row">@foreach(['x'=>'Left (%)','y'=>'Top (%)','w'=>'Width (%)','h'=>'Height (%)'] as $key=>$label)<div><label for="field-{{ $key }}">{{ $label }}</label><input type="number" id="field-{{ $key }}" data-property="{{ $key }}" min="{{ in_array($key,['x','y']) ? 0 : ($key==='w'?5:3) }}" max="{{ in_array($key,['x','y']) ? 95 : 100 }}" step="0.1"></div>@endforeach</div>
<label for="field-size">Font size (pt)</label><input type="number" id="field-size" data-property="size" min="6" max="48" step="0.5"><label for="field-align">Text alignment</label><select id="field-align"><option value="left">Left</option><option value="center">Centre</option><option value="right">Right</option></select>
<p class="help field-note">Use the arrow keys on a selected field to move it; hold Shift for larger steps. The QR code stays visible on a white background, at least 25 mm square.</p>
<div id="field-inputs"></div><button class="btn wide" type="submit">Save design</button><p id="save-status" class="help" role="status">Saved design ready for PDF.</p><p class="help">Saved designs can be reused by other events in your company.</p>
</section>
<section class="panel"><p class="eyebrow">03 · Print</p><h2>Print attendees</h2><p class="help">Save changes before downloading. PDFs use the same field positions, fonts and badge dimensions as this preview.</p><label for="print-category">Category</label><select id="print-category"><option value="">All confirmed attendees</option>@foreach($categories as $category)<option>{{ $category }}</option>@endforeach</select>
<label for="print-paper">Paper layout</label><select id="print-paper"><option value="badge">One badge per page</option><option value="a4">A4 sheet</option></select><p class="help" id="paper-help"></p><label class="check"><input type="checkbox" id="cut-guides" checked> Add cutting guides on A4</label>
<label for="attendee-search">Find attendees</label><input type="search" id="attendee-search" placeholder="Search names"><label class="check"><input type="checkbox" id="select-all" checked> Select all matching attendees</label><div class="attendee-list" id="attendee-list">@foreach($registrations as $registration)<label class="check" data-name="{{ $registration->participant->name }}" data-category="{{ $registration->participant->category }}"><input type="checkbox" value="{{ $registration->id }}" checked>{{ $registration->participant->name }}</label>@endforeach</div>
<p id="print-count" class="help"></p><button type="button" id="sample-pdf" class="btn secondary wide">Download sample PDF</button><button type="button" id="download-pdf" class="btn wide">Download selected badges</button><p class="help">Print at 100% / actual size. A sample uses the first selected attendee.</p><div id="print-error" class="warning" role="alert"></div>
</section>
</aside>
</div></form>
@if($templates->isNotEmpty())<form class="panel templates" method="POST" action="{{ route('events.badges.settings',$event) }}">@csrf @method('PATCH')<h2>Reuse a company template</h2><p class="help">Copy a saved design from another event. This replaces this event's badge design; attendee details come from this event.</p><label for="template">Saved event design</label><select name="template_id" id="template">@foreach($templates as $template)<option value="{{ $template->id }}">{{ $template->title }}</option>@endforeach</select><button class="btn secondary wide">Apply company template</button></form>@endif
@php
    $previewData = $registrations->mapWithKeys(fn($r) => [$r->id => ['values'=>\App\Support\BadgeDesign::values($event,$r), 'fullName'=>$r->participant->name, 'qr'=>route('events.badges.qr',[$event,$r])]]);
@endphp
<script>
window.badgeStudio = {
    fields: @json(old('badge_fields',$fields)),
    defaults: @json(\App\Support\BadgeDesign::defaults()),
    headerDefaults: @json(\App\Support\BadgeDesign::defaults('image_header')),
    labels: @json(\App\Support\BadgeDesign::LABELS),
    attendees: @json($previewData),
    artwork: @json($event->badge_image_path ? Storage::url($event->badge_image_path) : null),
    pdfUrl: @json(route('events.badges.pdf',$event)),
    hasErrors: @json($errors->any())
};
</script><script src="{{ asset('js/badge-studio.js') }}" defer></script>
</body></html>
